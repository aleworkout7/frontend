<%= form_for @product, validate: true do |f| %>
<% if @product.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

	<ul>
		<% @product.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="field">
	<%= f.label "Nombre de producto" %><br>
	<%= f.text_field :name %>
</div>
<div class="field">
	<%= f.label "Precio" %><br>
	<%= f.number_field :price, value: number_to_currency(f.object.price.to_f, delimiter: '', unit: ''), step: :any, required: true, min: 0 %>
</div>
<div class="field">
	<%= f.label "Escoga la tienda en la que agregará el Producto" %><br>
	<%= f.collection_select :shop_id, @shops, :id, :name %>
</div>
<br />
<div class="actions">
	<%= f.submit "Listo", class: 'button button3 btn-block', role: 'button' %>
</div>
<% end %>
