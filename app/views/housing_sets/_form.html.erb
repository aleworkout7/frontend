<hr>

<%= form_for(@housing_set) do |f| %>
	<% if @housing_set.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@housing_set.errors.count, "error") %> prohibited this city from being saved:</h2>

			<ul>
				<% @housing_set.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="form-group">
		<%= f.label :name %><br>
		<%= f.text_field :name, class:"form-control black-border", required: true %>
	</div>
	<div class="form-group">
		<%= f.label :district %><br>
		<%= f.collection_select :district_id, @districts, :id, :name, { prompt: "Selecione um Bairro" }, { class: 'form-control black-border', required: true } %>
	</div>
	<div class="actions">
		<%= f.submit "Salvar", class: "btn btn-success" %>
	</div>
<% end %>

<hr>
